
设定 3 种成分（A、B、C）的高斯特征参数以及真实浓度 $\mathbf{x}_{true} = [0.5, 1.0, 0.8]^T$。遍历 10 个采样波长（400nm - 670nm），计算生成 $10 \times 3$ 的系统响应矩阵。


任务二思路：
1.设定 4 组递增的噪声强度标准差 $\sigma = \{0.001, 0.005, 0.01, 0.05\}$。
2.利用矩阵乘法得出理想无噪信号 $\mathbf{y}_{true}$，并叠加正态分布噪声 $\boldsymbol{\eta}$ 生成观测信号 $\mathbf{y}$。
3.使用 numpy.linalg.lstsq 函数求解超定方程组的最小二乘解 $\hat{\mathbf{x}}$。
4.计算反演误差（真实值4.与估计值的 L2 范数：$||\hat{\mathbf{x}} - \mathbf{x}_{true}||_2$），并将结果汇总输出。